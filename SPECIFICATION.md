# 運命診断アプリ — 仕様書

**バージョン:** 3.b.1
**最終更新:** 2026-07-25

---

## 1. 概要

バイオリズムと四柱推命（四柱八字・通変星・十二運・五行）を組み合わせた運命診断Webアプリケーション。個人のコンディション診断（5運勢スコア含む）と二人の相性診断の2モードを提供する。相性診断では「基本相性」と「指定日の相性」の2段階で診断。週間・月間の運勢カレンダー、バイオリズム周期グラフ、画像付き結果共有機能、ラッキー要素表示も搭載。

---

## 2. システム構成

```
[ユーザーのブラウザ]
    ↓ POST（JSON）
[GitHub Pages]         ← index.html（フロントエンド）
    ↓ fetch API
[Vercel]               ← api/diagnose.js（サーバーレス関数）
    ↓ 命式計算: 命名サービス /api/bazi をHTTP呼び出し（節入りの天文計算・出生地の経度/均時差補正・早子時に対応）
[命名サービス Vercel]   ← /api/bazi（四柱＋五行バランスのみ返す軽量エンドポイント。LLM/DB不使用）。失敗時はローカル簡易計算にフォールバック
    ↓ 四柱を受領し、通変星・十二運はアプリ側で再計算
    ↓ LLM呼び出し（LLM_FALLBACK_ORDERで指定した順に試行。既定 Ollama→OpenRouter→Gemini）
[自宅Ollama]           ← 外部公開URL経由（Cloudflare Tunnel等）。応答不可/エラー時は次のプロバイダへ
[OpenRouter]
[Gemini（OpenAI互換エンドポイント）]
    ↓ 診断テキスト返却（最初に成功したプロバイダの結果を使用）
[Vercel → GitHub Pages → ブラウザに結果表示]
```

### 2.1 構成ファイル

| ファイル | 配置先 | 役割 |
|---|---|---|
| `index.html` | GitHub Pages | フロントエンド（UI・表示・入力・グラフ描画・画像生成） |
| `api/diagnose.js` | Vercel | バックエンド（命式は命名サービス `/api/bazi` をHTTP参照して取得。通変星・十二運・日運・5運勢はアプリ側で計算。取得失敗時はローカル簡易計算にフォールバック。LLM呼び出し） |
| `package.json` | Vercel | `"type": "module"` |
| `vercel.json` | Vercel | `maxDuration: 30` |
| `api/bazi.ts` | 命名サービス側リポジトリ / 命名サービス Vercel | 命式計算エンジン（四柱＋五行を返す軽量API。四柱推命アプリの `diagnose.js` からHTTP参照。LLM/DB不使用） |

### 2.2 環境変数（Vercel）

| 変数名 | 用途 |
|---|---|
| `OLLAMA_BASE_URL` | 自宅Ollamaを外部公開したURL（例: Cloudflare Tunnel等。`https://`込み、末尾スラッシュ不要）。未設定ならOllamaを試行せずスキップ |
| `OLLAMA_MODEL` | 自宅Ollamaのモデル名（未設定時 `llama3.1`） |
| `OLLAMA_TIMEOUT_MS` | 自宅Ollama応答待ちタイムアウト（未設定時 `8000`。自宅サーバー停止時に長時間待たないための保険） |
| `OPENROUTER_API_KEY` | OpenRouterのAPIキー。未設定ならスキップ |
| `OPENROUTER_MODEL` | OpenRouterのモデル名（未設定時 `meta-llama/llama-3.1-8b-instruct:free`） |
| `GEMINI_API_KEY` | Google AI StudioのAPIキー。未設定ならスキップ |
| `GEMINI_MODEL` | Geminiのモデル名（未設定時 `gemini-2.5-flash`）。OpenAI互換エンドポイント経由で呼び出し |
| `LLM_FALLBACK_ORDER` | LLMの試行順をカンマ区切りで指定（例: `gemini,ollama,openrouter`）。未設定時の既定は `ollama,openrouter,gemini`。未知の名前は無視。部分指定時は残りを既定順で自動補完。キー/URL未設定のプロバイダは順序に含めても自動スキップ |
| `ALLOWED_ORIGIN` | CORS許可オリジン（GitHub PagesのURL） |
| `NAMING_SERVICE_URL` | 命名サービス `/api/bazi` のベースURL（未設定時 `https://naming-service-red.vercel.app`）。命名サービス本番URLが異なる場合のみ設定 |
| `BAZI_TIMEOUT_MS` | `/api/bazi` 呼び出しのタイムアウトms（未設定時 `6000`）。超過・失敗時はローカル簡易計算にフォールバック |

**注意:** `OLLAMA_BASE_URL`にはVercel（クラウド）から到達可能な公開URLを指定する必要がある。自宅LANのプライベートIP（`192.168.x.x`等）は到達不可のため、Cloudflare Tunnel・Tailscale Funnel・ngrok等で外部公開したURLを使用する。

---

## 3. 機能仕様

### 3.1 診断モード

| モード | 発動条件 | スコア | 計算式 |
|---|---|---|---|
| 個人診断（solo） | 二人目の生年月日が空 | ○○点（0-100） | バイオリズム60% + 日運40% |
| 相性診断（pair） | 二人分入力済み | ○○%（0-100） | バイオリズム85% + 五行ボーナス + 通変星ボーナス + 日運ボーナス + 基礎点15 |

### 3.2 入力項目

| 項目 | 必須 | 形式 | 備考 |
|---|---|---|---|
| 一人目 名前 | 任意 | テキスト | 未入力→「あなた」/「Aさん」 |
| 一人目 性別 | 任意 | 選択 | 未選択/男性/女性 |
| 一人目 生年月日 | **必須** | YYYY/MM/DD | デフォルト：当日の20年前 |
| 一人目 出生時間 | 任意 | HH:MM | 時柱計算に使用 |
| 一人目 出生地 | 任意 | 選択（47都道府県） | **出生時間の入力時のみ有効**。経度・均時差補正に使用。未選択なら補正なし |
| 二人目（同上） | 任意 | 同上 | 二人目カードは**折りたたみ式**（既定は折りたたみ。保存された二人目の生年月日があれば自動展開）。未入力/折りたたみ→個人診断 |
| 二人目 出生地 | 任意 | 選択（47都道府県） | 同上（出生時間の入力時のみ有効） |
| 判定日 | 任意 | YYYY/MM/DD | 未指定→送信時のクライアント当日を明示送信 |

### 3.3 入力UI仕様

全フィールドが `type="text"` + `inputmode="numeric"` で統一。

| フィールド | テンキー | 自動挿入 | 全選択 | ピッカー |
|---|---|---|---|---|
| 生年月日（2つ） | ○ | スラッシュ | ○ | 隠しdate input + showPicker() |
| 出生時間（2つ） | ○ | コロン | ○ | 隠しtime input + showPicker() |
| 出生地（2つ） | — | — | — | 47都道府県の`<select>`（幅は都道府県名に合わせ、注釈は右側に配置し折り返し）。任意・未選択可 |
| 判定日 | ○ | スラッシュ | ○ | 隠しdate input + showPicker() |

紫色SVGアイコンをタップするとOS標準のカレンダー/時計ピッカーが起動。テキスト部分をタップするとテンキーで手入力可能。送信時にスラッシュ→ハイフンに自動変換（API側はYYYY-MM-DD受け取り）。判定日が空欄の場合は `null` を送らず、送信時点のクライアント当日（ローカル時刻ベースで `YYYY-MM-DD` を組み立て）を明示送信する。これによりページを過去に開いたまま放置していても占い実施日が正しく使われ、空欄時にペイロードが同一化することによるキャッシュ再利用を防ぐ。当日組み立ては `getFullYear/getMonth/getDate` を用い、`toISOString()` のUTC変換による日付ロールオーバーを回避（フロントはローカル基準の方針と整合）。

### 3.3.1 入力内容の保持（LocalStorage）

入力の手間を減らすため、フォームの入力内容を `localStorage`（キー `unmei_saved_inputs_v1`）に自動保存し、次回アクセス時に復元する。

- **保持対象:** `nameA, genderA, birthA, timeA, placeA, nameB, genderB, birthB, timeB, placeB`（10項目。出生地2つを含む）
- **保持しない項目:** 判定対象日（`targetDate`）。判定日は都度その日を占う想定のため、意図的に保存・復元しない
- **保存タイミング:** 各フィールドの `input`/`change`、およびピッカー選択時（`saveInputs`）
- **復元タイミング:** `DOMContentLoaded` 時に `loadSavedInputs` を実行。空文字の保存値では上書きしない。`birthA` は保存値があればそれを優先し、なければ従来どおり「当日の20年前」をデフォルト表示
- **堅牢性:** プライベートモード等で `localStorage` が使えない場合や保存データ破損時は `try/catch` で握りつぶし、通常動作を継続

### 3.3.2 二人目カードの折りたたみ

二人目の入力欄は既定で折りたたまれ、「二人目を追加して相性診断（任意）」のトグルで開閉する（`setCardB`）。`localStorage` に二人目の生年月日が保存されている場合は `DOMContentLoaded` 時に自動展開する。折りたたみ中は二人目の入力を無視して個人診断（solo）として扱う。

### 3.3.3 診断ボタンの活性制御

一人目・二人目のいずれの生年月日も空のときは「診断する」ボタンを非活性にする（`updateDiagnoseBtn`）。二人目は展開中のみ判定対象。生年月日が入力されると自動的に活性化する。

### 3.3.4 出生地の有効条件

出生地は**出生時間が入力されているときのみ**命式計算に反映される（経度・均時差補正）。出生時間が空のときは出生地セレクトを淡色にし、注釈を「出生時間があるときのみ有効（任意）」に切り替える（`syncPlaceState`）。注釈の色・サイズは出生時間の注釈（`.optional-hint`）と共通。

### 3.4 タイムゾーン処理

- フロントから `Intl.DateTimeFormat().resolvedOptions().timeZone` を送信
- バックエンドで `toLocaleDateString("en-CA", { timeZone })` で「今日」を判定
- **バックエンドの全 `new Date(dateStr)` を `new Date(dateStr + "T00:00:00Z")` に統一**（UTC零時基準）
  - 日数差計算は「日付の差」であり正午補正は不要。UTC零時基準の方がタイムゾーン問題が生じない
  - 対象: バイオリズム計算（`diffDays` 呼び出し）、`buildRangeData`、`buildBioGraphData`、`buildMeishiki`、`calcDayPillar`
- フロントエンドの曜日表示（ラッキーDAY・アンラッキーDAY行）はローカル時刻基準のため `T12:00:00`（タイムゾーンなし）を維持
- 命式計算は `getUTCFullYear()` / `getUTCMonth()` / `getUTCDate()` を使用
- 基準日は `Date.UTC(1900, 0, 1)` でUTC指定
- 日数加算はミリ秒演算（`baseMs + i * 86400000`）で `setDate()` を不使用
- **UTC+14〜UTC-10の全TZで同一結果になることを検証済み**

---

## 4. 判定ロジック

### 4.1 バイオリズム

| リズム | 周期 | 計算式 |
|---|---|---|
| 身体 | 23日 | `sin(2π × days / 23)` |
| 感情 | 28日 | `sin(2π × days / 28)` |
| 知性 | 33日 | `sin(2π × days / 33)` |

個人診断: sin値（-1〜+1）を0〜100%にスケーリング
相性診断: 二人のsin値の差から一致度を算出 `(1 - |差| / 2) × 100`

### 4.2 四柱推命 — 命式計算

> **命式エンジン（3.a.7〜）:** 命式（四柱＋五行バランス＋月支蔵干）は命名サービスの `/api/bazi` をHTTP参照して取得する。命名サービス側は節入りを**天文計算**（見かけの太陽黄経）で求め、出生地の**経度補正＋均時差**、**早子時**（23時台は翌日の日柱）に対応した高精度エンジン。四柱推命アプリ側は取得した四柱の上で**通変星・十二運を再計算**する（命名サービスは五行しか持たないため）。呼び出しに失敗した場合は、以下に記す**ローカル簡易計算**（節入り固定テーブル・時差補正なし）にフォールバックして診断を継続する。以下はフォールバック時のローカル計算仕様。

#### 四柱八字

- **年柱:** 立春（2/4頃）基準で年切り替え。干支60周期
- **月柱:** 節入り日テーブル（簡易）で月切り替え。月支は節切り月から算出（暦月mの節後の月支idx=m%12、寅=立春月）、月干は五虎遁（年干group×2+2＋寅からの経過月）で導出。子月・丑月の年またぎ境界も正しく処理
- **月支蔵干:** 月律分野蔵干テーブルを保持。節入りからの経過日数で余気→中気→本気の作用蔵干を決定し、蔵干の通変星を算出、五行バランスにも月令として加算
- **日柱:** 1900-01-01基準（offset=10）、`Date.UTC` + `getUTCFullYear` でUTC統一
- **時柱:** 2時間単位の十二支、日干から時干を導出（未入力時はnull）

#### 通変星（10種）

日干と他の天干の差（mod 10）から算出。

```
比肩 → 劫財 → 食神 → 傷官 → 偏財 → 正財 → 偏官 → 正官 → 偏印 → 印綬
```

#### 十二運（12種）

日干×地支のテーブル参照。各日干の長生の地支を起点に、陽干は順行・陰干は逆行で配当する。長生の地支は 甲=亥・乙=午・丙戊=寅・丁己=酉・庚=巳・辛=子・壬=申・癸=卯。

```
長生 → 沐浴 → 冠帯 → 建禄 → 帝旺 → 衰 → 病 → 死 → 墓 → 絶 → 胎 → 養
```

#### 五行バランス

天干+地支の合計6〜8要素から木・火・土・金・水の出現数をカウント。

### 4.3 日運計算

指定日の日柱を算出し、命式に対する影響を3要素で計算。

| 要素 | 重み | 内容 |
|---|---|---|
| 十二運スコア | 50% | 帝旺100〜絶10 |
| 通変星スコア | 35% | 食神85〜偏官35 |
| 五行相性スコア | 15% | 50 + gogyoBonus×5 |

日運スコア分布: 最小20〜最大95、平均58、100張り付き0件

#### 五行の日運影響

| 関係 | 説明 | ボーナス |
|---|---|---|
| 比和 | 同じ五行 | +8 |
| 相生（生じられる） | 追い風 | +10 |
| 泄気 | 消耗 | -3 |
| 相剋（剋される） | 試練 | -6 |
| 克出 | 活動的 | +3 |

### 4.4 5運勢スコア（個人診断時）

通変星・十二運・バイオリズム・五行バランス・性別から算出（0〜100）。

| 運勢 | 主な影響因子 |
|---|---|
| 金運 | 偏財(+20)、正財(+15)、金の五行数 |
| 恋愛運 | 食神(+15)、正財(+15)、性別補正 |
| 仕事運 | 正官(+20)、印綬(+15) |
| 健康運 | 食神(+10)、偏官(-10)、身体バイオリズム |
| 対人運 | 食神(+15)、劫財(-10)、水の五行数 |

### 4.5 相性判定

#### 五行相性

| 関係 | ボーナス |
|---|---|
| 相生 | +12 |
| 比和 | +8 |
| 相剋 | -5 |

#### 通変星相性

- 好相性（+5）: 食神×正財、正官×印綬、偏財×偏官 他
- 刺激的（-3）: 比肩×偏官、傷官×正官 他
- それ以外（0）

### 4.6 ラッキー要素算出（`calcLucky`）

命式の日干と五行バランスから7種のラッキー要素を算出。

#### 算出ロジック

| 要素 | 基準 |
|---|---|
| 日干 (`dayElement`) | 命式の日柱天干から五行を取得 |
| 補助五行 (`supportElement`) | 日干を「生む」五行（相生の前の行） |
| 弱五行 (`weakElement`) | 命式全体で最も出現数が少ない五行 |

**弱五行の決定ルール:**
- 最少出現数の五行を選択
- 同数の場合、日干と一致するものは避けて別要素を優先
- 全て同数かつ日干しか選択肢がない場合は補助五行に変更

#### 出力フィールド

| フィールド | 内容 | 基準 |
|---|---|---|
| `color` | ラッキーカラー（複数可） | 補助→日干→弱の順に五行カラーを重ね重複除去 |
| `number` | ラッキーナンバー（最大3個） | 日干+弱の数字を重複除去 |
| `direction` | ラッキー方角 | 弱→日干の順（重複除去） |
| `item` / `material` | ラッキーアイテム | 弱五行優先 |
| `food` | ラッキーフード | 弱五行優先 |
| `time` | ラッキータイム | 日干の時間帯 |
| `day` | ラッキーデー（曜日） | 日干の曜日 |
| `note` | 備考 | 「日干の○を活かし、不足しがちな○を補う」 |

#### 五行別マッピング

| 五行 | カラー | 数字 | 方角 | アイテム | フード | 時間帯 | 曜日 |
|---|---|---|---|---|---|---|---|
| 木 | 緑 | 3・8 | 東 | 植物・木製品 | 酸っぱいもの・緑の野菜 | 寅〜卯（3〜7時） | 木曜・水曜 |
| 火 | 赤・紫 | 2・7 | 南 | 照明・赤い小物 | 苦いもの・赤い食材 | 巳〜午（9〜13時） | 火曜 |
| 土 | 黄・ベージュ | 5・0 | 中央 | 陶器・天然石 | 甘いもの・黄色い食材 | 丑・辰・未・戌の時間帯 | 土曜 |
| 金 | 白・金色 | 4・9 | 西 | 金属小物・アクセサリー | 辛いもの | 申〜酉（15〜19時） | 金曜・月曜 |
| 水 | 水色・青 | 1・6 | 北 | 飲み物・水晶 | 塩辛いもの・黒い食材 | 亥〜子（21〜1時） | 水曜 |

---

## 5. AI診断文

### 5.1 使用モデル

利用可能なプロバイダは以下の3つ。試行順は`LLM_FALLBACK_ORDER`環境変数で指定でき、最初に成功したプロバイダの結果を採用する（既定順 `ollama,openrouter,gemini`）。

| プロバイダ | モデル | 備考 |
|---|---|---|
| 自宅Ollama | `OLLAMA_MODEL`環境変数（既定 `llama3.1`） | 未到達・エラー・タイムアウト時は次のプロバイダへ |
| OpenRouter | `OPENROUTER_MODEL`環境変数（既定 `meta-llama/llama-3.1-8b-instruct:free`） | |
| Gemini | `GEMINI_MODEL`環境変数（既定 `gemini-2.5-flash`） | OpenAI互換エンドポイント（`/v1beta/openai/chat/completions`）経由で呼び出し |

**試行順（`LLM_FALLBACK_ORDER`）の仕様:** カンマ区切りで指定（例: `gemini,ollama,openrouter`）。未知の名前は無視。部分指定時は残りを既定順で自動補完（例: `openrouter,gemini`→末尾に`ollama`を補完）。キー/URLが未設定のプロバイダは順序に含めても自動スキップする。

3プロバイダともOpenAI互換のchat completions形式（`/v1/chat/completions`または上記Gemini互換パス）で呼び出す。返り値の`usedModel`は`"ollama:モデル名"`/`"openrouter:モデル名"`/`"gemini:モデル名"`の形式。フロントエンドのfooter「Powered by」欄に、実際に使われたプロバイダ名とモデル名を診断結果ごとに表示する（`updatePoweredByFooter`関数）。

### 5.2 API設定

- temperature: 0.7、max_tokens: 3072
- 自宅Ollama: タイムアウト`OLLAMA_TIMEOUT_MS`（既定8秒）で見切り、429リトライは行わない（自宅環境では通常発生しないため）
- OpenRouter: タイムアウト15秒、429時は2秒待機して1回リトライ
- Gemini: タイムアウト15秒、429時は2秒待機して1回リトライ
- いずれも失敗した場合は`AI APIエラー: {詳細}。数分後に再試行してください。`を返す。すべての環境変数が未設定の場合は`AI API未設定`エラーを返す

### 5.3 プロンプト統一文

冒頭: 「あなたは四柱推命とバイオリズムに精通した占いライターです。以下のデータに基づいて、わかりやすい言葉で運勢を伝えてください。専門用語（通変星、十二運、五行、相生、相剋など）は使わず、その意味を日常的な表現に置き換えてください。データにない情報は書かないでください。」

言葉遣い: 「専門用語は一切使わない。「通変星に食神が」→「楽しいことに恵まれやすい運気が」、「十二運が帝旺」→「エネルギーがピークに近い状態」のように、意味だけを伝える。」

### 5.4 個人診断プロンプト構成（500〜700字）

1. コンディション一言まとめ（1文）
2. 生まれ持った性格の傾向（2文）
3. この日の運気の流れ（2文）
4. 5運勢で好調/注意の項目（2文）
5. 体力・気分・頭の回転（2文）
6. 具体的アドバイス（2文）

### 5.5 相性診断プロンプト構成（2セクション、`===SEPARATOR===`で分離）

**セクション1: 基本相性（250〜400字）** — 性格の噛み合い、コミュニケーション特徴
**セクション2: 指定日の相性（250〜400字）** — 運気の影響、バイオリズム波長、アドバイス

### 5.6 出力制御

| 制御 | 内容 |
|---|---|
| 形式 | プレーンテキストのみ、マークダウン禁止 |
| 日付表現 | 常に「この日は」に統一（「今日」「本日」禁止） |
| サニタイズ | マークダウン記号除去 + 「今日/本日」→「この日」置換（常時適用） |
| 途切れ対策 | finish_reason=length時に短縮リトライ + 文末補完 |

---

## 6. フロントエンド

### 6.1 デザイン

- **明るい和風モダン（3.a.7〜）**。生成り（washi）基調の明色テーマ。従来のダークモードから全面刷新
- フォント: Zen Maru Gothic / Shippori Mincho / Outfit
- カラー: **朱×紺**基調＋五行カラー併用。アクセント=朱`--accent:#c0604a`、副アクセント=紺`--accent2:#2f5670`
- CSS変数（抜粋）: `--bg:#e7dcc6` `--surface:rgba(255,252,246,.92)` `--surface2:#f1ead8`（入力欄） `--text:#2b2620` `--text2:#61574a` `--text3:#948974`
- 結果画面は**五行彩**（レーダー各頂点を五行色で色分け、点数リングは金→琥珀、バイオ棒グラフは身体=緑/感情=朱/知性=紺の単色）。暗背景・白文字を廃し墨色ベースでコントラスト確保
- 「診断する」ボタンはやわらかい朱グラデ（`#c0604a`→`#a04a37`）。両生年月日が空のとき非活性
- 最小フォントサイズ: 13px

### 6.2 タブUI（スコア直下、4タブ）

| タブ | 内容 |
|---|---|
| この日 | 命式表 + 日運 + 相性分析 + AI診断文 |
| この週 | 7日テーブル + コメント + ✨🌧️バッジ |
| この月 | 30日テーブル + コメント + ✨🌧️バッジ |
| バイオリズム | Canvas波形グラフ（前後15日） |

タブ切替時に `scrollIntoView({behavior:'smooth',block:'start'})` でタブバーを画面上部に固定。

### 6.3 結果表示（個人診断）

1. タイトル（名前 + 改行 + yyyy年M月d日）
2. 総合スコア（SVGリングアニメーション + カウントアップ）
3. バイオリズムバー（身体・感情・知性）
4. **5運勢レーダーチャート**（Canvas五角形、金/恋愛/仕事/健康/対人）
5. タブUI → 「この日」内に命式表 + 日運 + **ラッキー要素チップ** + AI診断

### 6.4 結果表示（相性診断）

1. タイトル + スコア + バー
2. レーダーチャート非表示
3. タブUI → 「この日」内に命式表(2人分) + **基本相性** + 指定日相性 + **ラッキー要素チップ（Aさん・Bさん各1セット）**

### 6.4.1 footer「Powered by」の動的表示

診断実行のたびに、APIレスポンスの`usedModel`（`"プロバイダ:モデル名"`形式）を`updatePoweredByFooter`関数で整形し、footer内`#poweredByModel`要素に反映する。表示形式は「{プロバイダ表示名} ({モデル名})」（例: `Ollama (llama3.1)`、`OpenRouter (meta-llama/llama-3.1-8b-instruct:free)`、`Gemini (gemini-2.5-flash)`）。ページ読み込み直後（診断未実行時）は初期値「AI」を表示する。

### 6.5 週間・月間タブ

#### 表示範囲

- **週:** 判定日から7日間
- **月:** 判定日から**31日間**（判定日が起点＝1行目）

#### ペアのスコア内訳表示（3.b.1〜）

ペア診断の週・月テーブルは、各日のスコアを**「相性／基本／総合」の3列**に分けて表示する（数字は等幅・右揃えで縦位置を統一）。

- **相性** … その日のバイオリズム相性＋日運（＝従来の各日スコア。日ごとに変動）
- **基本** … 二人の基本相性ボーナス（五行相性＋通変星相性。命式で決まる固定値。相剋・刺激的では負値もあり）。API の `baseCompatBonus` を使用
- **総合** … `min(100, 相性＋基本)`。判定日の総合はリング（%）と一致する

テーブル上部に「スコア＝その日の相性＋二人の基本相性＝総合（上限100）」の注釈を表示。ラッキー/要注意DAY・当日の強調、コメントは**総合スコア基準**で判定する。ソロ診断は従来どおり「星＋スコア」列（基本相性の概念なし）。

#### ラッキーDAY / アンラッキーDAY

期間内最高スコアの日に ✨、最低スコアの日に 🌧️ をテーブル行とバッジで表示。**ラッキーDAY行は淡い金地＋左に山吹の色帯、要注意DAY行は淡い藍地＋左に藍の色帯**で強調。バッジ・ラッキー要素チップは明色テーマで視認できる地色に調整（3.a.8）。

**当日（判定日）の強調（3.a.9〜）:** 判定日の行は**赤（朱）枠で全周を囲み**、淡い朱地＋太字で目立たせる。アイコン列には当日を示す赤丸マーカーを表示。ただし判定日がラッキー/要注意DAYにも該当する場合は、**そちら（✨/🌧️・金/藍の地色と色帯）の表示を優先**し、赤枠は残す（地色はラッキー/要注意が上書き、枠は判定日のまま）。

#### コメント生成（天気予報風）

**週間（平均スコア基準）:**
- avg≥60: ☀️ 好調
- avg≥50: ⛅ まずまず
- avg≥40: 🌥️ おとなしめ
- それ以外: ☁️ 蓄える時

**月間（max/min/spread基準、実データ分布に基づく閾値）:**

| パターン | 条件 | 出現率 |
|---|---|---|
| 🌈 絶好調日あり | max≥80 | 35% |
| 🎢 ジェットコースター | spread≥50 | 13% |
| 🌤️ 良い流れ | max≥70 かつ min≥30 | 28% |
| 🌥️ 慎重に | min≤25 | 4% |
| ⛅ 穏やか | それ以外 | 20% |

実データ分布: spread中央値48、max中央値76、min中央値29

### 6.6 バイオリズムグラフ

- Canvas描画、ウィンドウリサイズ追従
- 前後15日（31日分）の身体/感情/知性の波形
- 相性診断時は二人分を破線で重ねて表示
- 判定日に紫の縦破線マーカー
- 日付ラベル: 日の数字のみ、月変わりだけ「M/d」、幅ベースで動的間引き
- グリッド線: `rgba(70,55,40,.13)` ラベル: `rgba(43,38,32,.85) bold`（明色テーマ）
- 折れ線: 身体=緑`#6a9c5a`／感情=朱`#c0604a`／知性=紺`#2f5670`（相性時の二人目は淡色破線）

### 6.7 5運勢レーダーチャート（個人診断時）

- Canvas五角形、スコアリングとタブの間に表示
- 各頂点に運勢名（五行カラー）と数値
- グリッド線: `rgba(70,55,40,.16)` 軸線: `rgba(70,55,40,.13)`
- データ領域: `rgba(120,110,80,.16)` 線: `#8a7d55` 太さ2.5px（中立色）
- 各頂点は五行色で色分け（金=銀鼠`#74797f`／恋愛=朱`#b04d37`／仕事=紺`#2f5670`／健康=緑`#5a8a49`／対人=山吹`#b0842f`）

### 6.8 待機アニメーション

- フルスクリーンオーバーレイ（backdrop-filter: blur(8px)）
- SVGシャープ猫（8色パレットで1.8秒ごとランダム色変更）
- テキスト8種ローテーション + プログレスバー（90%まで漸増）

### 6.9 用語解説モーダル

- 「？用語を知る」ボタンで開閉
- 9項目: 四柱推命、四柱八字、天干と地支、通変星、十二運、五行、五行バランス、日運、バイオリズム
- 各項目に五行カラーのドットアイコン

### 6.10 アプリ内ブラウザ警告

User-Agent検出（FBAN/FBAV/Line/LIFF/Instagram/Twitter）で赤バナー表示。「『…』メニュー（右上または右下）から『ブラウザで開く』を選択してください」と案内。✕で閉じ可能。

### 6.11 共有機能

「この日」タブ選択時のみ表示。4つのボタン:

| ボタン | 動作 |
|---|---|
| X（短縮版） | X投稿画面（ヘッダー + 5運勢 + 診断80字 + URL） |
| LINE | **モバイル**: `line.me/R/msg/text/?` スキームで送信（300字 + URL）<br>**PC**: テキストをクリップボードにコピーし「LINEに貼り付けてください」トースト表示 |
| 全文コピー | スマホ: Web Share APIで**画像+テキスト**共有。PC: クリップボードに画像+テキスト |
| 📷 画像保存 | PNG画像をダウンロード |

**LINE PC判定ロジック:** `navigator.userAgent` に `iPhone/iPad/iPod/Android` を含まない、かつ `Macintosh` + `maxTouchPoints > 1`（iPad偽装Mac）でもない場合をPCと判定。

### 6.12 共有画像（Canvas動的生成）

PNG。**2パス方式**で生成する：まず十分大きい暫定高さ（`baseH+520`）のオフスクリーンに全要素を描画し、描画終端（`curY`＋フッター）を実測してから、その高さでトリミングした最終キャンバスにコピーして出力する。これにより内容量が変わっても縦位置の重なり・下部の見切れが起きない。

内容:
1. 円グラフ（総合スコア、リングゲージ）
2. 棒グラフ（バイオリズム3値、角丸バー）
3. レーダーチャート + 5運勢横棒（個人診断時のみ）
4. ラッキー要素チップ（相性診断は2名分）
5. **命式ブロック**（4柱表：時・日・月・年の順で、天干・地支・通変星・十二運・月支蔵干を表示。日柱の通変星は「(日主)」。下部に五行バランスのミニバー。**相性診断ではAさん・Bさん両名の命式を上下に描画**。時刻未入力で時柱が無い場合、時柱の天干・地支セルには薄い「—」を表示）
6. **この日の運気ブロック**（この日の干支・日運の通変星・十二運・日運スコアバー。**相性診断では両名分を並べて描画**）

各ブロックは描画関数（`drawMeishikiBlock`／`drawFortuneRow`）が「描画後の次のY」を返し、呼び出し側がそれを積み上げてレイアウトする。

配色（3.a.7〜・明色）: 背景#f3ecdd、ラベル#4a4238、数値#2b2620、トラック#e7ddc9、グリッド rgba(70,55,40,.18)、天干#b4703a、地支#2f5670、五行/レーダーは画面と同じ五行色

### 6.13 ラッキー要素表示

「この日」タブ内、命式表の直下に表示。

| 表示要素 | 内容 |
|---|---|
| セクションタイトル | 「✦ ○○のラッキー要素」（名前付き） |
| チップ形式 | 絵文字 + テキストの横並びチップ（折り返しあり） |
| 備考 | 五行の補足説明を小テキストで表示 |

**チップ一覧（7種）:**

| 絵文字 | 内容 |
|---|---|
| 🎨 | ラッキーカラー |
| 🔢 | ラッキーナンバー |
| 🧭 | ラッキー方角 |
| ✨ | ラッキーアイテム |
| 🍽️ | ラッキーフード |
| 🕒 | ラッキータイム |
| 📅 | ラッキーデー |

相性診断時はAさん・Bさんの2セット分を縦に並べて表示。

---

## 7. API仕様

### 7.1 リクエスト

```
POST /api/diagnose
Content-Type: application/json
```

| フィールド | 型 | 必須 | 備考 |
|---|---|---|---|
| nameA | string | 任意 | 未指定時「あなた」(solo)/「Aさん」(pair) |
| birthA | YYYY-MM-DD | **必須** | |
| genderA | "male"/"female"/null | 任意 | 恋愛運補正 |
| timeA | HH:MM/null | 任意 | 時柱計算 |
| placeA | 都道府県コード("01"〜"47")/null | 任意 | 出生地。出生時間ありのときのみ経度/均時差補正に使用 |
| nameB, birthB, genderB, timeB, placeB | 同上 | 任意 | 二人目カード展開時のみ有効 |
| targetDate | YYYY-MM-DD/null | 任意 | 空欄時はフロントがクライアント当日を明示送信。null時のみバックエンドがサーバーTZ当日で補完（保険） |
| timezone | IANA TZ名 | 任意 | デフォルト"Asia/Tokyo" |
| mode | "solo"/"pair" | 任意 | 自動判定 |

### 7.2 レスポンス

共通: `overallScore`, `physical`, `emotional`, `intellectual`, `meishikiA`, `fortuneA`, `dayPillar`, `diagnosis`, `usedModel`, `targetDate`, `weeklyData`(7件), `monthlyData`(30件), `bioGraph`(31件)

個人診断追加: `fiveScores`({money,love,work,health,social}), `lucky`(ラッキー要素オブジェクト)

相性診断追加: `meishikiB`, `fortuneB`, `gogyoRelation`, `tsuhenCompat`, `baseCompatBonus`（基本相性ボーナス＝五行相性＋通変星相性の合計。総合スコアに加算済みの固定値）, `baseDiagnosis`, `dailyDiagnosis`, `luckyA`, `luckyB`(各ラッキー要素オブジェクト)

### 7.3 エラー

| HTTP | 条件 |
|---|---|
| 400 | 生年月日不足 |
| 405 | POST以外 |
| 500 | APIキー未設定 |
| 502 | Gemini全モデル失敗 |

### 7.4 命式エンジンAPI（命名サービス側 `/api/bazi`）

四柱推命アプリの `diagnose.js` がサーバー間で呼び出す軽量エンドポイント（命名サービスのVercelプロジェクトに配置）。LLM・DBは使わない。

```
POST /api/bazi
Content-Type: application/json
{ "birthDate": "YYYY-MM-DD", "birthTime": "HH:MM"(任意), "birthPlace": "01"〜"47"(任意), "timezone": "Asia/Tokyo"(任意) }
→ { "meishiki": { year, month, day, time|null, dayElement, monthZoukan, gogyoCount, level, earlyZishiApplied, timeShiftMinutes } }
```

- `GET /api/bazi` は都道府県一覧（`{code,name}`）を返す
- `birthDate` のみで判定可能（時柱・時差補正は任意）。未入力項目は補完しない
- 通変星・十二運は含まない（アプリ側で四柱から再計算）

---

## 8. バックエンド関数一覧（17関数）

| 関数 | 役割 |
|---|---|
| `handler` | エントリポイント |
| `buildMeishiki` | 命式取得（命名サービス `/api/bazi` をHTTP参照。失敗時 `buildMeishikiLocal` へフォールバック。async） |
| `buildMeishikiLocal` | 四柱八字のローカル簡易算出（フォールバック用。旧 `buildMeishiki`） |
| `enrichFromRemoteMeishiki` | `/api/bazi` の四柱に通変星・十二運を付与し既存形状へ整形 |
| `calcDayPillar` | 指定日の日柱算出 |
| `calcDailyFortune` | 日運計算 |
| `calcFiveFortuneScores` | 5運勢スコア算出 |
| `calcLucky` | ラッキー要素算出（五行ベース） |
| `getTsuhen` | 通変星算出 |
| `getTsuhenCompatibility` | 通変星相性判定 |
| `getGogyoRelation` | 五行相性判定 |
| `buildRangeData` | 週間/月間データ生成（週=判定日から7日、月=判定日から31日） |
| `buildBioGraphData` | バイオリズムグラフデータ生成 |
| `buildSoloPrompt` / `buildPairPrompt` | プロンプト構築 |
| `callAI` | LLM呼び出しの統括（`LLM_FALLBACK_ORDER`に従い各プロバイダを順に試行） |
| `callOpenAICompatible` | OpenAI互換chat completions呼び出し（Ollama/OpenRouter/Gemini共通処理） |
| `sanitizeDateWords` | 日付表現サニタイズ |
| `formatMeishiki` | 命式テキスト変換 |
| `diffDays` / `calcBiorhythm` | 日数差・バイオリズム計算 |

---

## 9. フロントエンド関数一覧（33関数）

| 関数 | 役割 |
|---|---|
| `runDiagnosis` | 診断実行 |
| `displaySolo` / `displayPair` | 結果表示制御 |
| `switchTab` | タブ切替 |
| `drawBioGraph` | バイオリズムグラフ描画 |
| `drawRadar` | レーダーチャート描画 |
| `renderRangeTable` | 週間/月間テーブル生成 |
| `buildRangeComment` | コメント生成 |
| `showRangeTabs` | データ投入 |
| `generateShareImage` | 共有画像生成 |
| `shareResult` | 媒体別共有 |
| `downloadShareImage` | 画像ダウンロード |
| `openPicker` | ピッカー起動 |
| `populatePrefSelects` | 出生地セレクト（47都道府県）生成 |
| `syncPlaceState` | 出生時間の有無で出生地の有効/注釈を切替 |
| `setCardB` | 二人目カードの折りたたみ制御 |
| `updateDiagnoseBtn` | 生年月日の有無で診断ボタンの活性制御 |
| `renderMeishiki` / `renderGogyoBalance` | 命式表生成 |
| `renderFortuneDay` / `renderFortuneCards` | 日運表示 |
| `renderCompat` | 相性カード |
| `renderLucky` | ラッキー要素チップ表示 |
| `fmtDateFull` / `fmtDateShort` | 日付フォーマット |
| `showLoading` / `hideLoading` / `changeNekoColor` | 待機アニメーション |
| `setScore` / `setBars` / `setBar` / `animateNumber` | スコアアニメーション |
| `showResult` / `showError` / `showToast` | UI制御 |
| `esc` / `toggleShareMenu` / `buildShareText` / `roundRect` | ユーティリティ |

---

## 10. コスト

| サービス | 費用 |
|---|---|
| GitHub Pages | 無料 |
| Vercel (Hobby) | 無料 |
| Google AI Studio | 従量課金（月$0.10〜0.30程度） |

---

## 11. テスト結果

| テスト項目 | 結果 |
|---|---|
| 命式計算（時柱あり/なし） | OK |
| 立春境界（2/3 vs 2/5） | OK |
| 日運計算（スコア範囲20-95） | OK |
| 5運勢スコア（範囲5-100） | OK |
| 五行相性（相生/比和/相剋） | OK |
| 通変星相性（好相性/刺激的） | OK |
| 週間データ（7件、solo/pair） | OK |
| 月間データ（30件） | OK |
| バイオリズムグラフ（31件、中央=判定日） | OK |
| 日付サニタイズ（本日/今日→この日） | OK |
| 日運スコア分布（100張り付き0件） | OK |
| HTML全ID要素の存在 | OK |
| JS全関数の存在 | OK |
| TZ検証（UTC/JST/EST/UTC+14/UTC-10 全同一結果） | OK |
| 月間コメント分布（5パターンに分散） | OK |
| ラッキー要素算出（全5日干×全フィールド） | OK |
| ラッキー重複方角・弱五行同数ケース | OK |

---

## 12. 開発履歴

### バージョニング規約

マイナー番号は16進数で採番する。`3.a.0`（従来採番の 3.10.0 相当）を起点に、マイナーアップは `a → b → c → d → e → f` と16進で繰り上げる。**`3.f.0` の次はメジャーアップとし、`4.0.0` とする**（`3.g.0` は使用しない）。フロントエンド修正時は必ずバージョンを繰り上げる。**`index.html` 内の2箇所（フッターの `ver 3.a.x` 表記と `const APP_VERSION`）を両方更新すること。**

| バージョン | 主な変更 |
|---|---|
| **3.b.1** | **ペア診断の週・月テーブルのスコアを「相性／基本／総合」の3列に分割表示し、二人の基本相性ボーナス（五行＋通変星の固定加点）を可視化。総合＝相性＋基本（上限100）で判定日はリングの%と一致。注釈を追加。API pairレスポンスに `baseCompatBonus` を追加。ソロは変更なし** |
| **3.b.0** | **マイナー版数を繰り上げ（3.a.7〜3.a.10 の変更＝命名サービス命式エンジン連携・出生地入力・二人目カード折りたたみ・明るい和風テーマ／結果画面の五行彩配色・当日行の強調などを集約）。機能変更なし** |
| **3.a.10** | **月タブの表示期間を「判定日から31日間」に戻す（3.a.8で導入した『判定日中心の前14日＋後16日＝30日』を撤回。週は変更なし）** |
| **3.a.9** | **週・月タブの当日（判定日）行を赤枠で全周を囲み淡い朱地＋太字＋当日マーカーで強調。判定日がラッキー/要注意DAYに該当する場合はそちらの地色・色帯を優先しつつ赤枠は残す** |
| **3.a.8** | **月タブの表示範囲を「判定日から30日」→「判定日の前14日＋以降16日＝計30日」（判定日を中央付近に）へ変更（`buildRangeData` に `startOffset` を追加、週タブは変更なし）。結果画面の視認性改善: ラッキー要素チップとラッキーDAY/要注意DAYバッジの地色を明色テーマで見やすく調整し、テーブルのラッキー/要注意日に行の色地＋左の色帯を追加** |
| **3.a.7** | **命式計算を命名サービスの高精度エンジン（`/api/bazi`）へHTTP参照で一元化（節入りの天文計算・出生地の経度/均時差補正・早子時。通変星・十二運はアプリ側で再計算、失敗時はローカル簡易計算にフォールバック）。フロントに出生地セレクト（47都道府県・任意、出生時間ありのときのみ有効）を追加。二人目カードを折りたたみ式に（保存値で自動展開）。両生年月日が空のとき診断ボタンを非活性化。UIを明るい和風テーマ（朱×紺）へ全面刷新し、結果画面（レーダー・点数リング・バイオ棒グラフ・折れ線・共有画像・命式表）を五行彩の明色配色にしてコントラスト改善** |
| **3.a.6** | **共有画像の命式・この日の運気ブロックの縦位置崩れ（五行バランスと見出しの重なり）を修正。固定高さ見積もりを廃し、描画関数が次のYを返す積み上げ方式＋2パス方式（実測後トリミング）に変更。相性診断でBさんの命式・この日の運気も画像に追加（両名分描画）。時刻未入力の時柱は薄い「—」表示** |
| **3.a.5** | **十二運テーブルの誤りを修正（丁・己・庚・辛・壬・癸の6日干で長生の地支・順逆がずれ十二運が全滅していたバグ。甲乙丙戊は正しかった）。あわせて共有画像に「命式」ブロック（4柱表＋通変星＋十二運＋月支蔵干＋五行バランス）と「この日の運気」ブロック（干支＋日運の通変星＋十二運＋スコア）を追加。画像高さを内容に応じ拡張** |
| **3.a.4** | **LLMの試行順を環境変数`LLM_FALLBACK_ORDER`で指定可能に。プロバイダ定義をオブジェクト化してリファクタ。カンマ区切りで順序指定（未知名は無視、部分指定は既定順で補完、未設定プロバイダは自動スキップ）。既定順は従来どおり`ollama,openrouter,gemini`。挙動・出力は従来と互換** |
| **3.a.3** | **AIフォールバックにGeminiを第3段として追加（Ollama→OpenRouter→Gemini、GeminiはOpenAI互換エンドポイント経由）。フロントエンドfooterの「Powered by」を静的表記から動的表記に変更し、診断結果ごとに実際に使用したプロバイダ・モデル名を表示（`updatePoweredByFooter`）** |
| **3.a.2** | **AI診断コメント生成をGemini APIから自宅Ollama（メイン）→OpenRouter（フォールバック）方式に変更。OpenAI互換のchat completions形式に統一。Ollamaは応答タイムアウト（既定8秒）で見切りOpenRouterへ切替、OpenRouterは429時2秒待機で1回リトライ。プロンプト構成・出力後処理（マークダウン除去・途切れ時短縮リトライ・文末補完）・呼び出し元コードは変更なし** |
| **3.a.1** | **入力内容のLocalStorage保持機能を実装。判定対象日（targetDate）を除く8項目（名前・性別・生年月日・出生時間×2名分）を自動保存し、次回アクセス時に復元。プライベートモード等では例外を握りつぶして通常動作を継続** |
| **3.a.0** | **月柱計算バグ修正（月支・月干が1つずれていた問題を全面修正、年またぎ境界含む）＋月支蔵干（月律分野蔵干）実装。節入りからの経過日数で余気/中気/本気を判定し、蔵干の通変星算出・五行バランス反映・診断プロンプトへの追加を行う** |
| **3.9.2** | **判定日空欄バグ修正: ページを過去に開いたまま放置時、空欄でも読み込んだ日で占われる問題を修正。空欄時はフロントが送信時のクライアント当日を明示送信（null送信を廃止）。fetchに `cache:'no-store'`、APIレスポンスに `Cache-Control: no-store, no-cache, must-revalidate` を追加しキャッシュ再利用を防止** |
| **3.9.1** | **LINE共有のPC対応修正: PC環境ではクリップボードコピー＋トースト表示に変更（モバイルのみlineスキーム使用）** |
| 3.9.0 | バイオリズム計算TZ修正: バックエンドの日付パースを `T12:00:00Z`→`T00:00:00Z` に統一 |
| 3.8.0 | ラッキー要素表示（calcLucky）追加。五行・日干ベースで色/数字/方角/アイテム/フード/時間/曜日を算出。共有画像高さ拡張（800/620px） |
| **3.7.0** | **ラッキー要素機能着手（calcLucky実装）。重複方角・弱五行同数バグ修正** |
| 3.6.0 | TZ再点検（getUTC系・Date.UTC基準日・ミリ秒加算統一）。5TZで検証済み |
| 3.5.0 | 隠しpicker inputでカレンダー/時計ピッカー復活。手入力とピッカー併用 |
| 3.4.0 | 判定日も手入力化。type=date完全廃止 |
| 3.3.0 | セパレータ統一（スラッシュ）。時間も手入力化 |
| 3.2.0 | 生年月日のスマホ手入力対応（type=text + 自動スラッシュ） |
| 3.1.0 | TZ完全修正（T12:00:00Z統一）。アプリ内ブラウザ警告 |
| 3.0.0 | 全ロジックテスト完了。月間コメント分岐修正。リリース版 |
| 2.7.0 | 配色全面見直し。共有ボタン「この日」タブ限定表示 |
| 2.6.0 | 画像に円グラフ+棒グラフ+レーダー統合。日付表示M月d日統一 |
| 2.5.0 | ラッキー/アンラッキーDAY。週間月間コメント。OG:Image全面改修 |
| 2.4.0 | 5運勢スコア。レーダーチャート。スマホWeb Share API画像共有 |
| 2.3.0 | タブ位置修正。グラフ描画タイミング修正。共有機能（媒体別） |
| 2.2.0 | 週間月間タブ。バイオリズムグラフ。診断文トーン平易化 |
| 2.1.0 | 日運計算。Flash/Flash-Liteフォールバック。用語モーダル。日運スコア修正。日付表現「この日」統一。途切れ対策 |
| 2.0.0 | 四柱推命本格化。命式表。猫アニメーション。TZ対応 |
| — | Geminiに切替。四柱推命追加。個人診断追加 |
| 初版 | バイオリズム相性診断。Claude API構成 |
